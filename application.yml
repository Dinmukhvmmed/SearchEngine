server:
  port: 8080

spring:
  datasource:
    username: root
    password: 1006991713dD
    url:
      jdbc:mysql://localhost:3306/search_engine?useSSL=false&requireSSL=false&allowPublicKeyRetrieval=true
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true

indexing-settings:
  sites:
    - url: https://ipfran.ru/
      name: ИПФ
    - url: https://dombulgakova.ru/
      name: ДомБулгакова
    - url: https://skillbox.ru/
      name: Skillbox